"""Factor sets for the cantilever wall tool -- one YAML file per framework.

Adding a framework is adding a file here plus an entry in
:data:`~austruct.tools.cantilever_wall.factor_sets.KNOWN_FRAMEWORKS`, not a
release. See :mod:`.factor_sets` for the loader and the :class:`FactorSet`
model every file validates into.
"""
